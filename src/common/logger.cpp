#include "common/logger.hpp"

// Logger is header-only (singleton), but we need a TU for the static lib.
// Future: file-based logging, syslog output, log rotation can go here.

namespace kirdi {

// Placeholder for future log sink implementations.

} // namespace kirdi
