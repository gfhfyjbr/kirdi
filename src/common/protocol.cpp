#include "common/protocol.hpp"

// Protocol is mostly header-only (inline functions).
// Complex protocol logic (e.g. packet reassembly, flow control) goes here.

namespace kirdi::protocol {

// Future: packet reassembly for fragmented IP packets,
// compression (zstd/lz4), and encryption layer.

} // namespace kirdi::protocol
