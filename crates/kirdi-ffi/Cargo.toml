[package]
name = "kirdi-ffi"
version = "0.1.0"
edition = "2021"
description = "Safe Rust wrapper around the kirdi VPN client C API"

[dependencies]
kirdi-sys = { path = "../kirdi-sys" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
